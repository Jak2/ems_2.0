Frontend (React + Vite)
-----------------------

Quick start:

```powershell
cd frontend
npm install
npm run dev
```

The UI is a simple upload form plus a prompt input that calls the backend endpoints at `http://localhost:8000`.
